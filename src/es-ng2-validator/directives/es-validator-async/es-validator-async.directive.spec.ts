import { EsValidatorAsyncDirective } from './es-validator-async.directive';

describe('EsValidatorAsyncDirective', () => {
  it('should create an instance', () => {
    const directive = new EsValidatorAsyncDirective();
    expect(directive).toBeTruthy();
  });
});
