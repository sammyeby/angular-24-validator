import { EsValidatorDirective } from './es-validator.directive';

describe('EsValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
