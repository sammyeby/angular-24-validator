import { EsValidatorMultiDirective } from './es-validator-multi.directive';

describe('EsValidatorMultiDirective', () => {
  it('should create an instance', () => {
    const directive = new EsValidatorMultiDirective();
    expect(directive).toBeTruthy();
  });
});
