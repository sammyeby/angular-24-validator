import { EsValidatorAsyncMultiDirective } from './es-validator-async-multi.directive';

describe('EsValidatorAsyncMultiDirective', () => {
  it('should create an instance', () => {
    const directive = new EsValidatorAsyncMultiDirective();
    expect(directive).toBeTruthy();
  });
});
